# POINTER — this workflow is INERT here: GitHub Actions only reads
# .github/workflows/ at the REPOSITORY root, and this plugin lives inside the
# saldigioia/claude-plugins monorepo. The live workflow is
#   <repo root>/.github/workflows/remuxing-to-mov-ci.yml
# (path-filtered to plugins/remuxing-to-mov/**; ffmpeg matrix 4.4/6.1/7.1/
# 8.1.2/9.0.1). If this plugin is ever extracted to its own repository, move
# that file back here and strip the plugins/remuxing-to-mov/ path prefixes.
# History: the original in-plugin workflow was dead-lettered from 1.0.0 through
# 1.14.0 (its own header said so); resurrected at the root in 1.15.0 Phase 0.
name: pointer (inert — see repo-root workflow)
on: workflow_dispatch
jobs:
  pointer:
    if: ${{ false }}   # never runs; see the header
    runs-on: ubuntu-latest
    steps:
      - run: "true"
