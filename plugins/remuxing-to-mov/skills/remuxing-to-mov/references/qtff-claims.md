# QTFF claim inventory (Phase 0 of QTFF-AUDIT-PLAN.md)

Every container-level or QuickTime-behavior assertion this plugin makes, stated
as a falsifiable sentence, with the most specific page of the local Apple QTFF
archive (`/Users/salvatore/quicktime-docs/`) it must be resolved against and a
concrete probe. **A divergence is a defect until every probe is individually
explained** (the post-mortem acceptance doctrine). Verification happens in
Phases 1–5; every row starts UNVERIFIED — no row may be flipped without probe
evidence recorded in the commit that flips it.

Paths in *Stated in* are relative to `skills/remuxing-to-mov/`
(`../mov/SKILL.md` is the companion `/mov` skill). Paths in *Spec anchor* are
relative to `/Users/salvatore/quicktime-docs/` (all verified to exist);
"player behavior — empirical only" marks claims the file-format spec cannot
decide.

**Bench moves.** Verdicts are a property of the bench they ran on — the
playable-check self-dating rule — and that covers ffmpeg as much as macOS.
On **2026-08-14**, mid-1.11-work-order, homebrew moved the bench **ffmpeg
8.1.2 → 9.0.1** (no v8 binary remains). Rows dated ≤2026-08-13 were benched
on 8.1.2 or older exactly as each row states and are NOT re-dated; the move
itself produced a claim (C74: `-vsync` removed, seam-check.sh broke and was
patched); every 1.11-sweep row (section F) and every re-verification dated
2026-08-14 ran on 9.0.1 / macOS 26.6.1. The full regression suite was
re-validated on 9.0.1 the same day.

### A. Timeline, timescale & sample tables

| # | Claim | Stated in | Spec anchor | Probe | Status |
|---|-------|-----------|-------------|-------|--------|
| C01 | A `-c copy` of video packets that carry no PTS/DTS produces a MOV whose muxer-invented timeline shows 1-tick `stts` durations, decode-order PTS, and negative/invented DTS — bit-perfect essence, unwatchable file. | SKILL.md; references/timeline-repair.md; scripts/pairfill-paff.sh | markdown/sample_atoms/time-to-sample_atom.md | Straight-copy a pair-timestamped PAFF fixture; `mp4dump --verbosity 2` the `stts`/`ctts`; ffprobe duration histogram (verify.sh d) | UNVERIFIED |
| C02 | TS→MOV keeps the source 90 kHz clock as the video `mdhd` timescale, so 59.94 fields/s yields `stts` durations of exactly 1501/1502 ticks (the assumption pairfill's histogram gate is built on). | references/timeline-repair.md; scripts/pairfill-paff.sh; scripts/verify.sh | markdown/media_atoms/media_header_atom.md; markdown/sample_atoms/time-to-sample_atom.md | `mp4dump` mdhd timescale + stts on a pairfill output, per CI ffmpeg version (4.4/6.1/7.1/8.x) | CONFIRMED 2026-07-25 on ffmpeg 8.1.2 (mdhd timescale=90000, stts 1501/1502 on a pairfill TS→MOV output); per-version pin still owed (5d) |
| C03 | A clean CFR broadcast remux produces a sample-duration histogram of only one or two adjacent values (e.g. 1501+1502), ±1 count. | references/verification-safety.md; scripts/verify.sh | markdown/sample_atoms/time-to-sample_atom.md | `ffprobe -show_entries packet=duration` histogram on remux.sh output of a CFR fixture | CONFIRMED 2026-07-25 on ffmpeg 8.1.2: pairfill output = 59x1501+60x1502+1 stray; plain CFR copies = a single value (gate (d) histograms across the suite) |
| C04 | The pts>dts composition offsets pairfill writes are representable in MOV `ctts` (spec: deltas "can be positive or negative") with `cslg` covering the shift, and QuickTime honors whichever form ffmpeg writes. | references/timeline-repair.md (reorder pyramid {0,3003,6006,9009,15015}); scripts/pairfill-paff.sh | markdown/sample_atoms/composition_offset_atom.md; markdown/sample_atoms/composition_shift_least_greatest_atom.md | `mp4dump` ctts version/signedness + cslg presence on a pairfill output; scrub in QuickTime Player | CONFIRMED 2026-07-25 on ffmpeg 8.1.2: unsigned v0 ctts SHIFTED by the preroll (min offset = preroll) + a two-entry elst (empty edit + MediaTime=preroll) — no cslg needed; QuickTime-Player scrub of the rebuilt T9 deliverables clean — user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5) |
| C05 | On a pairfill output, max(PTS−DTS) over ALL packets never exceeds the measured PREROLL (pyramid depth rounded to a whole pair) — the offset bound is derived, never violated. | scripts/pairfill-paff.sh (header: preroll default) | markdown/sample_atoms/composition_offset_atom.md | `ffprobe -show_entries packet=pts,dts` whole file; assert max delta ≤ preroll | BUG FOUND + FIXED 2026-07-25: a wrong-cadence DTS ramp (field-rate ramp on frame packets) drove max offset to 192192 ≫ preroll 12012 with growing ctts and mdhd ≠ Σstts — and every point gate passed. pairfill now gates max(PTS−DTS) ≤ preroll+pair AND decode-vs-presentation span skew ≤ 2 pairs; regression-pinned. BOUND REVISED 2026-07-26 (5-4d, gate ④): the fixed preroll+pair limit structurally refused legitimate ≥2-level hierarchical-B pyramids (C67; XLVI 36036 > 30030, unsatisfiable by any flag) — now max(PTS−DTS) ≤ PREROLL + min(measured pair-ramp PTS excursion, 16 pairs) + 1 pair, floored at preroll+pair so zero-excursion streams keep the old limit exactly; measured live on the read-only T7 pair: source excursion +9009 (3.00 pairs), deliverable max 36036 ≤ derived 39039. Wrong-cadence class still refused (16-pair clamp + unchanged 2-pair span-skew gate; regression pin holds, exit 1 "TIMELINE GATES FAILED") |
| C06 | ffmpeg writes an `elst` edit list for nonzero starts / negative initial DTS, and players honor it — a MPEG-2 copy-cut showing video `start_time` ≈ 0.45 s with audio at 0 is the keyframe/edit-list offset, not a sync defect. | references/dual-track-quicktime.md; references/container-internals.md (edts/elst optional) | markdown/track_atoms/edit_list_atom.md; markdown/track_atoms/playing_with_edit_lists.md | `mp4dump` elst on a copy-cut output; compare tkhd vs mdhd duration; play the head in QuickTime | CONFIRMED (mechanism) 2026-07-25 on ffmpeg 8.1.2: TS→MOV copy writes video elst (5 ms empty edit + media map); ffprobe stream durations = media durations, so the A/V parity gate measures the right quantity; QuickTime head-play pending |
| C07 | Edit lists with negative offsets cause playback stalls; `-avoid_negative_ts make_zero` flattens them away. | references/container-internals.md | markdown/track_atoms/edit_list_atom.md; markdown/track_atoms/playing_with_edit_lists.md | Build the same cut with and without `make_zero`; `mp4dump` elst media_time fields; play both | UNVERIFIED — annotated 2026-08-14 (WO 5.4, ffmpeg 9.0.1): no plugin script passes `-use_editlist` or `-movflags +negative_cts_offsets` (grep-verified across scripts/), so every route rides the mov muxer default (`auto`), which governs QuickTime initial-delay/priming presentation per ffmpeg-formats(1); candidate future knob. The stall claim itself gained no evidence either way |
| C08 | Raw PCM in MOV is a contiguous sample array at a fixed rate with **no gap mechanism**, so a dropped-frame gap has nowhere to live and collapses on copy. (Suspect: QTFF *empty edits* exist precisely for gaps — the defensible claim may only be "ffmpeg's muxer writes none per gap".) | SKILL.md; references/timeline-repair.md; scripts/resync.sh | markdown/track_atoms/edit_atom.md (empty edits); markdown/media_data_atom_types/sound_media/sound_sample_data.md | Copy a gapped fixture; `mp4dump` the audio trak for elst entries; count empty edits (expect 0 from ffmpeg, but the spec allows them) | REFINED 2026-07-25 on ffmpeg 8.1.2: the spec HAS the mechanism and ffmpeg USES it for initial offsets (a 0.4 s TS audio delay copied to MOV lands as a 394 ms empty edit, elst media time −1) — but writes no MID-stream empty edit per drop, which is where collapse happens. Doctrine conclusion stands; mechanism wording corrected in timeline-repair.md |
| C09 | The video track preserves a source's forward timestamp gap on copy (one outsized `stts` entry), so the video timeline still spans the true real-time duration. | references/timeline-repair.md | markdown/sample_atoms/time-to-sample_atom.md | `mp4dump` stts on a copy of a gapped fixture — expect one entry ≫ nominal duration | UNVERIFIED |
| C10 | A clean single-program remux has identical durations on every track; a per-track duration spread is the gap-collapse fingerprint (packetized AC-3/DTS/MP2 drift less than raw PCM). | references/timeline-repair.md; scripts/verify.sh (parity gate) | markdown/media_atoms/media_header_atom.md; markdown/track_atoms/track_header_atom.md | ffprobe per-stream durations on gapped-fixture remuxes with PCM vs AC-3 audio | UNVERIFIED |
| C11 | A 0.25 s A/V duration-parity tolerance is sound because AAC/AC-3 encoder delay (priming) legitimately shifts audio track durations via the spec's delay-representation structures. | scripts/verify.sh (RTM_SYNC_TOL); references/verification-safety.md | markdown/media_data_atom_types/sound_media/appendix_g_audio_priming_handling_encoder_delay_in_aac.md; …/historical_solution_implicit_encoder_delay.md; …/using_track_structures_to_represent_encode_delay_explictly.md | `mp4dump` elst/sgpd on an AAC dual-track output; measure the legitimate Δ against the tolerance | CONFIRMED (spec-bound) 2026-07-25 on ffmpeg 8.1.2: QTFF Appendix G pins implicit AAC priming at 2112 samples ≈ 44 ms @48 kHz — an order of magnitude under the 0.25 s gate; note added to verification-safety.md. Empirical alignment half already covered by the --audio md5 QC |
| C12 | An AC-3/E-AC-3 "delay relative to video" is carried in the timestamps and preserved by `-c copy` into MOV (landing as track start offsets/edit list); only the Rung-3 rebuild rezeros it. | references/verification-safety.md | markdown/track_atoms/edit_list_atom.md; markdown/track_atoms/playing_with_edit_lists.md | TS fixture with a known audio delay; ffprobe start_time + `mp4dump` elst on the output vs source | CONFIRMED 2026-07-25 on ffmpeg 8.1.2: a 0.4 s TS audio delay survives -c copy as an audio-track empty edit (elst: 394 ms media time −1) + preserved relative start_time (0.394 s) |
| C13 | An MKV 1 ms timebase over a long duration overflows QuickTime duration fields ("duration too long for timebase"); `-video_track_timescale` fixes the video track, and a `mov_text` track inheriting the 1 ms base can be the overflowing track with no per-subtitle knob. | references/timeline-repair.md; references/color-hdr-subs.md | markdown/media_atoms/media_header_atom.md; markdown/movie_atoms/movie_header_atom.md | Synthesize a long 1/1000-tb input; mux with/without the knob; `mp4dump` mdhd timescale/duration per track | UNVERIFIED |
| C14 | Rebuilding video and audio both from zero keeps them aligned because they shared the source `start_pts` (a genuine inter-stream offset needs `-itsoffset` reapplied). | references/timeline-repair.md; scripts/rebuild-paff.sh | player behavior — empirical only (track alignment at movie time 0) | Rebuild a fixture with known A/V offset; measure lip-sync vs source (cross-correlate audio, compare frame at t) | UNVERIFIED |
| C15 | `mvhd` timescale/duration "must agree with track `mdhd`" — the movie duration equals the longest track duration expressed in the movie timescale. | references/container-internals.md | markdown/movie_atoms/movie_header_atom.md; markdown/track_atoms/track_header_atom.md | `mp4dump` mvhd/tkhd/mdhd on remux outputs; check the actual consistency rule the spec states | CONFIRMED 2026-07-25 on ffmpeg 8.1.2: mvhd (ts=1000, dur=4010) = longest tkhd duration; video tkhd 4010 ms = mdhd 4004 ms + 5 ms initial edit; audio tkhd 4000 = mdhd 4000 |

### B. Seekability & random access

| # | Claim | Stated in | Spec anchor | Probe | Status |
|---|-------|-----------|-------------|-------|--------|
| C16 | A closed-GOP/IDR keyframe is a full sync sample (`stss`) — the spec's "self-contained … independent of preceding frames" — and is safe to start a segment on. | references/cutting-concat.md; scripts/gop-probe.sh | markdown/sample_atoms/sync_sample_atom.md | Read the page; `mp4dump` stss on a closed-GOP copy; cut there and seam-check | UNVERIFIED |
| C17 | An open-GOP I-frame is a partial sync sample (`stps`): a random-access point whose neighbors display earlier (`ctts`) and depend on the previous GOP (`sdtp` EarlierDisplayTimesAllowed) — not self-contained at a join. | references/cutting-concat.md; scripts/gop-probe.sh | markdown/sample_atoms/partial_sync_sample_atom.md; markdown/sample_atoms/sample_dependency_flags_atom.md | `mp4dump` stss/stps/sdtp on an open-GOP x264 fixture copied to MOV | UNVERIFIED |
| C18 | ffmpeg's MOV muxer actually writes `stps` (and `sdtp`) for open-GOP H.264 on copy, rather than marking everything sync in `stss` — the premise under gop-probe/seam-check's QuickTime grounding. | references/cutting-concat.md (implicit); scripts/gop-probe.sh (header) | markdown/sample_atoms/partial_sync_sample_atom.md; markdown/sample_atoms/sample_dependency_flags_atom.md | `x264 --open-gop` fixture → `-c copy` MOV → `mp4dump | grep -E 'stps|sdtp|stss'` per ffmpeg version | REFUTED 2026-07-25 on ffmpeg 8.1.2: open-GOP TS→MOV copy puts ALL non-IDR recovery I-frames in `stss` (full sync), writes no `stps`/`sdtp` — the container overclaims seekability. gop-probe correctly ignores the atoms (display-order test); doctrine corrected in cutting-concat.md |
| C19 | Mux-valid ≠ seekable: a file can pass strict-mux tests yet tear on scrub because the seek lands via `stss` + edit list; an accurate off-keyframe seek (`-ss` after `-i`) exercises the same path a QuickTime GUI scrub does, while a keyframe-snap seek does not. | references/verification-safety.md; SKILL.md; scripts/verify.sh (e) | markdown/sample_atoms/sync_sample_atom.md; markdown/track_atoms/playing_with_edit_lists.md; player behavior for the scrub-path equivalence | On a genpts'd broken PAFF fixture: `-ss` before `-i` (clean) vs `-ss` after `-i` (errors); scrub the same target in QuickTime Player | CONFIRMED: the incident record IS the broken half (mux-test-passing files tore in real playback); repaired T9 deliverables scrub clean — user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5) |
| C20 | A single GOP (or absurdly sparse `stss`) over a multi-minute file is effectively not seekable in a real player. | references/verification-safety.md; scripts/verify.sh (keyframe-spacing check) | markdown/sample_atoms/sync_sample_atom.md; player behavior — empirical only | Single-keyframe long fixture; measure scrub latency / decode-from-far-back in QuickTime | UNVERIFIED |
| C21 | A QuickLook thumbnail (qlmanage → AVFoundation, the same stack QuickTime Player uses) proves one frame decodes and nothing about the presentation timeline — a muxer-invented-timeline file still renders one. | scripts/playable-check.sh; references/verification-safety.md | player behavior — empirical only | Run playable-check.sh on the known-broken incident-class file (should pass) while verify.sh (d)/(e) FAILs it | CONFIRMED via the incident record: the shipped-broken files rendered QuickLook thumbnails while unwatchable — floor-not-sign-off doctrine encoded in playable-check.sh |

### C. Sample descriptions & signaling

| # | Claim | Stated in | Spec anchor | Probe | Status |
|---|-------|-----------|-------------|-------|--------|
| C22 | `colr` and `fiel` live inside the video sample entry (e.g. `avc1`) within `stsd`, not at the top level of the file. | references/container-internals.md ("verified") | markdown/media_data_atom_types/video_media/video_sample_description_extensions.md; markdown/media_data_atom_types/video_media/video_sample_description_extensions/color_parameter_atom.md | `mp4dump` tree on any remux output — colr/fiel nested under stsd/avc1 | CONFIRMED 2026-07-25 on ffmpeg 8.1.2: colr and pasp sit inside the avc1 sample entry under stsd (mp4dump tree); fiel not written (see C24) |
| C23 | Modern ffmpeg writes `colr` by default on a copy when the source is tagged (`+write_colr` redundant); in MOV the parameter type is the QTFF `nclc` form. | SKILL.md; references/color-hdr-subs.md; references/container-internals.md; references/verification-safety.md | markdown/media_data_atom_types/video_media/video_sample_description_extensions/color_parameter_atom.md | Remux a tagged fixture WITHOUT `+write_colr`; `mp4dump` for colr and its parameter type (nclc vs nclx) per ffmpeg version | CONFIRMED 2026-07-25 on ffmpeg 8.1.2: fully VUI-tagged source → colr written on copy WITHOUT +write_colr, parameter type nclc, payload 1/1/1 (bt709). Nuance: a PARTIALLY tagged source (matrix only, primaries/transfer unknown) gets NO colr — consistent with the don't-fabricate doctrine. Older-version pin via CI |
| C24 | ffmpeg writes no `fiel` atom on copy, and that is fine: field order is preserved via the bitstream (VUI/SEI) and read back correctly. (Suspect: the spec names `fiel` as the field-handling extension used "to modify decompressed image data … or determine field display order" — AVFoundation deinterlacing without it is unproven.) | references/timeline-repair.md; references/container-internals.md; references/verification-safety.md | markdown/media_data_atom_types/video_media/video_sample_description_extensions.md | `mp4dump` a PAFF deliverable for fiel; play in QuickTime and check deinterlacing/field order on motion | CONFIRMED user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5): the field-coded (tt) T9 deliverables carry no fiel and full-screen motion is smooth — no combing, no field-order shimmer. AVFoundation deinterlaces from the bitstream; no fiel injector needed |
| C25 | Full/limited range lives in the bitstream VUI (QTFF `nclc` carries no range flag) and survives copy. | references/color-hdr-subs.md | markdown/media_data_atom_types/video_media/video_sample_description_extensions/color_parameter_atom.md | ffprobe color_range source vs output on limited- and full-range fixtures | UNVERIFIED |
| C26 | HDR10 mastering-display and content-light metadata live in the HEVC SEI; ffmpeg writes no container-level `mdcv`/`clli` boxes on copy and `+write_colr` does not add them; SEI-reading players still get HDR10. | SKILL.md; references/color-hdr-subs.md | no QTFF page (mdcv/clli are ISO-BMFF-era boxes absent from the QTFF spec) — empirical | `mp4dump` an HDR10 HEVC copy for mdcv/clli; ffprobe -show_frames side data survives | UNVERIFIED |
| C27 | HEVC in MOV must carry the `hvc1` sample-entry fourcc to play in QuickTime; ffmpeg's default `hev1` is container-valid but unplayable there. | SKILL.md; ../mov/SKILL.md; references/ingest-compatibility.md; references/verification-safety.md; references/delivery-encode.md | player behavior — empirical only (neither fourcc is in the QTFF spec; structure per markdown/media_data_atom_types/video_media/video_sample_description.md) | Same file retagged hev1 vs hvc1; playable-check.sh + QuickTime open on both | CONFIRMED user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5): hev1 fixture does not play; the identical bits tagged hvc1 play |
| C28 | A sample description missing its decoder-config box (`avcC`/`hvcC`) makes the file undecodable. | references/container-internals.md | markdown/media_data_atom_types/video_media/video_sample_description_extensions/avc_decoder_configuration_atom.md | Strip avcC (MP4Box/hex) from a fixture; ffprobe/QuickTime both fail to decode | UNVERIFIED |
| C29 | MOV stores H.264 as `avc1` + `avcC` (AVCC length-prefixed): Annex-B TS input is converted automatically on copy-mux, and extraction back to Annex-B needs `h264_mp4toannexb`. | references/ingest-compatibility.md ("verified") | markdown/media_data_atom_types/video_media/video_sample_description_extensions/avc_decoder_configuration_atom.md; markdown/media_data_atom_types/video_media/video_sample_description.md | ffprobe `is_avc`/`nal_length_size` on a TS→MOV output; `mp4dump` avcC contents | CONFIRMED 2026-07-25 on ffmpeg 8.1.2: TS→MOV output is_avc=true, nal_length_size=4, avcC carries SPS/PPS |
| C30 | MOV PCM is legal in both endiannesses (`sowt`/`twos`) plus `in24` and the v2 `lpcm` form — all registered QTFF sound formats. | references/codec-landscape.md | markdown/media_data_atom_types/sound_media/sound_sample_descriptions.md | Mux pcm_s16le and pcm_s16be fixtures; `mp4dump` stsd fourcc; both play in QuickTime | CONFIRMED (structure) 2026-07-25 on ffmpeg 8.1.2: pcm_s16le → sowt; pcm_s24le → in24 v1 (see C31). in24 playback user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5) (kit fixtures played); twos half pending |
| C31 | Decoded 5.1 PCM (`pcm_s24le`, 6 ch) in MOV plays with correct L/R/C/LFE/Ls/Rs mapping in QuickTime — which per spec requires the v2 sound description plus a `chan` audio-channel-layout extension. (⚠ suspected live bug: all current fixtures are mono/stereo; the >2-ch path is untested.) | references/dual-track-quicktime.md ("LPCM, which QuickTime always plays"); scripts/dual-track.sh; scripts/pairfill-paff.sh; scripts/resync.sh | markdown/media_data_atom_types/sound_media/sound_sample_descriptions/sound_sample_description_version_2.md; markdown/media_data_atom_types/sound_media/sound_sample_description_extensions/audio_channel_layout_atom.md | dual-track.sh a 5.1 AC-3 fixture; `mp4dump` stsd version + chan; channel-ident tones per speaker, listen on a Mac | CONFIRMED 2026-07-25 on ffmpeg 8.1.2 (atoms): 6-ch PCM gets a QTFF v1 `in24` description (bytesPerFrame=18) WITH `chan` layout tag 0x790006 = kAudioChannelLayoutTag_MPEG_5_1_A, positionally 1:1 with ffmpeg 5.1(side); the copied AC-3 keeps its own `chan` bitmap 0x60F. Speaker-ident listen user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5): correct speaker per tone; older-ffmpeg pin still owed |
| C32 | MP2 muxes into MOV only under the non-standard tag `.mp2` — absent from the QTFF sound-format registry — which is why the mux succeeds while playback is not expected. | SKILL.md; references/ingest-compatibility.md ("verified"); references/codec-landscape.md | markdown/media_data_atom_types/sound_media/sound_sample_descriptions.md (absence of a Layer-2 fourcc) | Mux an MP2 fixture; `mp4dump` stsd fourcc; playable-check + listen | CONFIRMED (structure) 2026-07-25 on ffmpeg 8.1.2: stsd sample entry is the non-standard tag `.mp2` as claimed; playback user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5): QuickTime plays no MP2 audio, as documented |
| C33 | MP3 copies into MOV under `.mp3` — a registered QTFF sound format (`kFullMPEGLay3Format`) — and plays natively in QuickTime. | references/ingest-compatibility.md (verified 8.1.1); references/codec-landscape.md | markdown/media_data_atom_types/sound_media/sound_sample_descriptions.md | Mux an MP3 fixture; `mp4dump` stsd; listen in QuickTime Player | CONFIRMED (structure) 2026-07-25 on ffmpeg 8.1.2: stsd sample entry `.mp3` (registered QTFF format); listen user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5): plays |
| C34 | SubRip has no MOV carriage (mapping it aborts the mux at header write); text subtitles must become `mov_text` — QTFF subtitle media, sample description `tx3g` — or ride sidecar. | SKILL.md; references/ingest-compatibility.md (verified 8.1.1); references/color-hdr-subs.md; references/cutting-concat.md | markdown/media_data_atom_types/subtitle_media.md; markdown/media_data_atom_types/subtitle_media/subtitle_sample_description.md | `-map 0` with SRT (expect header-write failure); `-c:s mov_text` output `mp4dump` sbtl/tx3g | REFINED 2026-07-25 on ffmpeg 8.1.2: in a qt-brand MOV, mov_text lands as classic QT TEXT media (`text` sample entry + `text` handler), NOT tx3g — tx3g is the MP4 form. color-hdr-subs.md corrected; the no-SubRip-carriage half stands; subtitle displays in QuickTime Player (user-verified 2026-07-25) |
| C35 | Embedded EIA-608/708 captions live inside the video bitstream (A/53 user data / SCTE-128 SEI) and survive any video copy — but QuickTime *renders* captions from a `clcp` (c608) track, so "captions preserved" as ffprobe reports it may not mean "QuickTime shows captions". | references/color-hdr-subs.md; references/ingest-compatibility.md; scripts/verify.sh (--signaling) | markdown/media_data_atom_types/closed_captioning_media.md | Copy an A/53 CC fixture; ffprobe CC presence; toggle captions in QuickTime Player — do they display without a clcp track? | UNVERIFIED |
| C36 | ProRes profiles 0–5 produce exactly the sample-description tags `apco/apcs/apcn/apch/ap4h/ap4x`. | references/delivery-encode.md (verified 8.1.1); references/ingest-compatibility.md | no QTFF page (ProRes tags are not in the spec) — empirical (mp4ra registry) | Encode each prores_ks profile; `mp4dump` stsd fourcc per profile | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (WO 5.1 bench sweep): `-c:v prores_ks -profile:v N` for N=0..5 into `-f mov` reads back codec_tag_string exactly apco(Proxy)/apcs(LT)/apcn(Standard)/apch(HQ)/ap4h(4444)/ap4x(XQ). Re-verify: one 1 s lavfi encode per profile + `ffprobe -show_entries stream=codec_tag_string,profile` (tag read via ffprobe's stsd fourcc; mp4dump cross-check optional). Per-ffmpeg-version pin owed, as with C02 |

### D. Track structure, chapters & metadata

| # | Claim | Stated in | Spec anchor | Probe | Status |
|---|-------|-----------|-------------|-------|--------|
| C37 | Chapters survive `-c copy` into MOV: ffmpeg synthesizes a QuickTime chapter *text track* linked by a `chap` track reference from the A/V tracks. | SKILL.md (verified 8.1.1); references/ingest-compatibility.md | markdown/media_data_atom_types/chapter_lists.md; markdown/track_atoms/track_reference_atoms/track_reference_type_atom.md; markdown/media_data_atom_types/text_media.md | Remux a chaptered MKV; `mp4dump` for the text trak + `tref`/`chap`; chapter menu appears in QuickTime | CONFIRMED 2026-07-25 on ffmpeg 8.1.2 (structure): chaptered mux writes a `text`-handler track plus `tref`→`chap` on BOTH the video and audio traks; QuickTime chapter menu user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5) |
| C38 | The generic "second menu" some workflows show is that chapter text track — a data stream (`bin_data`, tag `text`) with `tref→chap` links — which QuickTime renders as a navigable chapter menu. | references/container-internals.md; scripts/metadata.sh | markdown/media_data_atom_types/chapter_lists.md | `mp4dump` a chaptered output; open in QuickTime and confirm the menu maps to that track | CONFIRMED user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5): chapter menu shows One/Two; no stray second track visible |
| C39 | Stripping the chapter track (`-map -0:d` + `-map_chapters -1`) removes the menu without leaving a dangling `chap` tref on the remaining tracks, and `--keep-chapters` keeps both halves (track AND tref). | scripts/metadata.sh; references/container-internals.md | markdown/track_atoms/track_reference_atoms/track_reference_type_atom.md; markdown/media_data_atom_types/chapter_lists.md | `mp4dump` metadata.sh output both ways; grep for tref/chap and orphaned track IDs | CONFIRMED 2026-07-25 on ffmpeg 8.1.2: strip removes the text track AND both `chap` trefs (no dangling reference); `--keep-chapters` keeps the text track, both trefs, and the chapter metadata (ffprobe shows both chapters) |
| C40 | The first mapped audio stream becomes the track QuickTime plays by default. | SKILL.md; references/verification-safety.md | markdown/track_atoms/track_header_atom/flags.md; markdown/track_atoms/track_header_atom/alternate_group.md; player behavior for "default" | Two-audio remux with distinct tones; open in QuickTime, which track sounds | UNVERIFIED (player half) — structure half benched 2026-08-14 under C75 (ffmpeg 9.0.1): movenc itself enables ONLY the first mapped audio track (tkhd flags 3, the rest 2) and groups all audio in alternate_group=1, so the "default" the claim names is the only *enabled* track; the discriminating-tone listen remains owed |
| C41 | In the dual-track deliverable (`-disposition:a:0 default -disposition:a:1 0`), QuickTime plays ONLY the PCM access track — the ISO disposition maps to QT enabled flags/alternate grouping such that the original track is silent until selected. (⚠ suspected live bug: QuickTime plays every *enabled* track; if both tracks land enabled and ungrouped, they MIX.) | references/dual-track-quicktime.md; scripts/dual-track.sh; scripts/mov.sh; scripts/pairfill-paff.sh; ../mov/SKILL.md | markdown/track_atoms/track_header_atom/flags.md; markdown/track_atoms/track_header_atom/alternate_group.md; markdown/structuring_movie_data_and_features/preparing_sound_and_subtitle_alternate_groups_for_use_with_apple_devices.md | `mp4dump` tkhd flags + alternate_group on a dual-track output; play once on a real Mac with different content on each track (tone vs speech) | SUSPICION REFUTED 2026-07-25 on ffmpeg 8.1.2 (atoms): tkhd flags = 3/3/2 — video + PCM access ENABLED, the preserved original track flags=2 (in-movie, NOT enabled), so QuickTime plays only the access track; confirmation listen done 2026-07-25: only the 440 Hz access track audible on the discriminating fixture — no mixing |
| C42 | `-metadata:s:a:0 language=eng` lands as the `mdhd` packed language code (ISO-639-2/T vs legacy Mac values per spec) and QuickTime/Finder display the track language from it. | SKILL.md; references/verification-safety.md | markdown/media_atoms/media_header_atom/language.md; markdown/basic_data_types/language_code_values.md | `mp4dump` mdhd language field on a tagged rebuild; Finder Get Info / QuickTime track list shows "English" | CONFIRMED (structure) 2026-07-25 on ffmpeg 8.1.2: language=eng lands as mdhd language 0x0000 = legacy Mac code English (spec-valid QTFF form, values <0x400) and ffprobe round-trips it as eng; display half user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5) |
| C43 | Proper QuickTime metadata is `udta/meta` with an `mdta` handler, a `keys` box of reverse-DNS names and a parallel `ilst` of values — the structure Apple tools and Finder read; ffmpeg writes it with `use_metadata_tags` + `com.apple.quicktime.*`. | references/container-internals.md; scripts/metadata.sh | markdown/metadata_atoms_and_types.md; markdown/metadata_atoms_and_types/metadata_atom.md; markdown/metadata_atoms_and_types/metadata_item_keys_atom.md; markdown/metadata_atoms_and_types/metadata_item_list_atom.md | `mp4dump` metadata.sh output: meta/hdlr=mdta/keys/ilst layout vs the spec pages; `mdls`/Finder read-back | CONFIRMED (structure) 2026-07-25 on ffmpeg 8.1.2: metadata.sh output carries meta → hdlr(mdta) + keys + ilst exactly per the spec layout; mdls/Finder read-back half remains |
| C44 | Legacy ©-atoms (`©nam`/`©des`/`©cmt`) sit directly under `udta` (what naive `-metadata title=` emits); QuickTime reads some of them — the older, lossier mapping. | references/container-internals.md | markdown/movie_atoms/user_data_atoms.md | Naive `-metadata title=X` mux; `mp4dump` udta; check what QuickTime/Finder display | CONFIRMED (structure) 2026-07-25 on ffmpeg 8.1.2: naive -metadata writes udta/©nam directly (no keys/ilst/mdta) — the legacy lossier mapping; display half remains |
| C45 | `moov` before `mdat` (faststart) is required for progressive/network playback; `moov` at EOF plays fine locally but starts poorly when streamed. | SKILL.md; references/container-internals.md | markdown/storing_and_sharing_media_with_quicktime_files/quicktime_movie_files.md; markdown/movie_atoms/interleaving_movie_data.md; partly player behavior | `mp4dump` atom order with/without `+faststart`; serve over HTTP and measure start latency | CONFIRMED (structure) 2026-07-25 on ffmpeg 8.1.2: +faststart → ftyp,moov,wide,mdat; without → ftyp,wide,mdat,moov. Latency half remains player-behavior |
| C46 | `stco` offsets are 32-bit, so a file whose media crosses 4 GiB needs `co64`; a fresh ffmpeg remux writes it automatically. | references/container-internals.md | markdown/sample_atoms/chunk_offset_atom.md | Build a >4 GiB output (or force with padding); `mp4dump` for co64 vs stco | CONFIRMED 2026-07-26 on the first real >4 GiB deliverable (XLVI feed.mov, 4,858,093,274 bytes, read-only on T7): mp4dump shows `co64` on all three traks (avc1 video + in24 access + AC-3 original — the dual-track pattern), zero `stco`. Round-2 blocked-on-artifact waiver lifted |
| C47 | The 64-bit extended atom size field is legal only when `size == 1`; an atom with size < header, overrunning the file, or overlapping regions is corruption/hostile input. | references/container-internals.md | markdown/storing_and_sharing_media_with_quicktime_files/atoms.md; markdown/storing_and_sharing_media_with_quicktime_files/quicktime_movie_files/movie_data_atom.md | Read the spec page against the stated rule; hex-inspect a >4 GiB mdat header | UNVERIFIED |
| C48 | A minimal valid MOV is `ftyp` (major brand `qt  `) + exactly one `moov` (mvhd + ≥1 trak with a complete sample table) + ≥1 `mdat`; a truncated `moov` is undecodable with no in-place fix. | references/container-internals.md | markdown/storing_and_sharing_media_with_quicktime_files/quicktime_movie_files.md; markdown/storing_and_sharing_media_with_quicktime_files/quicktime_movie_files/file_type_compatibility_atom.md | `mp4dump` a remux output for the structure + brand; truncate moov on a scratch copy and confirm undecodable | UNVERIFIED |
| C49 | A wrong handler subtype (`soun` on a video track) breaks playback. | references/container-internals.md | markdown/media_atoms/handler_reference_atom.md | Hex-patch hdlr on a scratch fixture; QuickTime/ffprobe behavior | UNVERIFIED |
| C50 | Fragmented captures (`moof` after `moov`, sample tables per-fragment `traf`, A/V drift when `tfdt` is missing) are defragmented into a single `moov`+`mdat` by a fresh `-c copy` remux, after which the normal workflow applies. | references/container-internals.md | no QTFF page (moof/traf/tfdt are ISO BMFF, absent from the QTFF spec) — empirical | Remux a CMAF/DASH capture; `mp4dump` output for zero moof, single moov | UNVERIFIED |
| C51 | `sinf`/`schi`/`tenc`/`pssh` boxes mean DRM/CENC content — no remux or decode without keys. | references/container-internals.md | no QTFF page (CENC boxes absent from the QTFF spec) — empirical | `mp4dump` a FairPlay/CENC sample; confirm ffmpeg copy produces nothing usable | UNVERIFIED |
| C52 | `-timecode HH:MM:SS:FF` writes a QuickTime timecode track (`tmcd`, hdlr `tmcd`, tref from video) that QuickTime/editorial tools read. | references/delivery-encode.md; references/container-internals.md (hdlr vide/soun/tmcd) | markdown/media_data_atom_types/timecode_media.md; markdown/track_atoms/track_reference_atoms/track_reference_type_atom.md | ProRes fixture with `-timecode`; `mp4dump` tmcd trak + tref; QuickTime shows the TC | CONFIRMED (structure) 2026-07-25 on ffmpeg 8.1.2: -timecode writes a tmcd track with tmcd handler + tref→tmcd from the video track; QuickTime display half remains |

### E. Player behavior (empirical only)

| # | Claim | Stated in | Spec anchor | Probe | Status |
|---|-------|-----------|-------------|-------|--------|
| C53 | E-AC-3 (Dolby Digital Plus, incl. JOC) plays natively in modern QuickTime/macOS — the single-track copy is the correct deliverable. | SKILL.md; ../mov/SKILL.md; references/ingest-compatibility.md; references/verification-safety.md; scripts/mov.sh | player behavior — empirical only | E-AC-3 fixture → remux; play + listen in QuickTime on target macOS versions | CONFIRMED user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5): plays natively |
| C54 | AC-3 in MOV plays on modern macOS QuickTime but is spotty on older versions — the reason AC-3 is dual-tracked while E-AC-3 is not. | SKILL.md; references/ingest-compatibility.md; references/verification-safety.md | player behavior — empirical only | AC-3-only single-track MOV on current + one older macOS; listen | CONFIRMED (modern half) user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5): plays; the older-macOS spottiness stays historical rationale for the dual-track default |
| C55 | DTS and DTS-HD MA copy into MOV (tag `dtsc`) but QuickTime will not play them. | references/ingest-compatibility.md ("verified"); references/verification-safety.md; references/codec-landscape.md | player behavior — empirical only (`dtsc` absent from the QTFF sound registry: markdown/media_data_atom_types/sound_media/sound_sample_descriptions.md) | DTS fixture copy; `mp4dump` stsd; playable-check + listen | CONFIRMED user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5): video plays, audio silent |
| C56 | MPEG-2 4:2:0 in MOV plays in QuickTime; 4:2:2 (422@HL) generally does not. | references/ingest-compatibility.md; references/verification-safety.md; SKILL.md (Rung 4) | player behavior — empirical only | 4:2:0 and 4:2:2 MPEG-2 fixtures in MOV; playable-check.sh both on target macOS | CONFIRMED user-verified in QuickTime Player 2026-07-25 (macOS Darwin 25.5): 4:2:2 does not play; 4:2:0 plays. **REVERSED (4:2:2 half) 2026-08-13 on macOS 26.6.1 / ffmpeg 8.1.2**: the same class now fully decodes — qlmanage thumbnail (playable-check.sh) AND avconvert whole-file (50/50 frames) — so the claim's "generally does not" is a property of the macOS it was measured on, not of the codec (C63's drift direction runs BOTH ways: support can be restored as well as dropped). The 4:2:0 half is unaffected; the 26.5.2-era failure record above stays (a registry records reversals, it doesn't erase them). Consequence: the 1.8.0–1.10.0 categorical refusal gate was demoted to a per-file post-build empirical check (WO 4.1; see C72). 4:2:0 half re-confirmed as the clean control class 2026-08-14 on macOS 26.6.1 / ffmpeg 9.0.1: remux.sh m2v420.ts → DONE, playable-check OK. **NARROWED (reversal half) 2026-08-15 on macOS 26.6.1 / ffmpeg 9.0.1**: the 2026-08-13 "fully decodes" proved *renders*, not *renders correctly* — two real 1080i59.94 broadcast 4:2:2 masters (`m2v1`) returned playable-check `verdict=ok` on the same OS while QuickTime rendered macroblock garbage; per-file correctness is now measurable (`playable-check.sh --fidelity`, C96); and the very 4:2:2 failure this row records is resolved by the m2v1→xd5* dispatch remedy (C97) — the same bitstream retagged `xd5b` renders correctly, losslessly |
| C57 | Legacy QuickTime-7-era codecs (Cinepak `cvid`, Sorenson `svq3`, …) remain container-valid but AVFoundation cannot decode them since macOS Catalina. | references/ingest-compatibility.md | player behavior — empirical only | A cvid/svq3 MOV; ffprobe decodes, playable-check.sh fails on modern macOS | UNVERIFIED. NOTE (5-1b, 2026-07-26): the Tahoe 26.4 codec-drop reports (July 2026 research sweep; forum threads on MJPEG-variant/AIC removals) corroborate the drift direction — annotation only, forum evidence does not meet the probe bar. The cvid/svq3 fixture probe is still owed; the ADJACENT drift claim (C63) now carries first-party measured data (4:2:0/4:2:2 MJPEG split on 26.5.2) |
| C58 | ProRes plays natively on macOS but not on iOS devices. | references/codec-landscape.md; references/delivery-encode.md | player behavior — empirical only | ProRes MOV on a Mac (plays) and an iOS device (does not) | UNVERIFIED |
| C59 | Vorbis muxed into MOV on ffmpeg 8.1.1 gets the non-standard tag `msVo` and QuickTime will not play it (refused outright on 6.1.1). | references/ingest-compatibility.md; references/codec-landscape.md | player behavior — empirical only (tag absent from markdown/media_data_atom_types/sound_media/sound_sample_descriptions.md) | Vorbis mux per ffmpeg version; `mp4dump` stsd; playable-check | UNVERIFIED |
| C60 | The QTFF *container* can carry AV1/Opus/FLAC (Mediabunny writes them bit-exact into `qt`-brand MOV and ffmpeg demuxes the result) — ffmpeg's four hard rejections are write-side policy, not container physics; QuickTime plays none of them. | references/ingest-compatibility.md (verified 2026-06-10); references/codec-landscape.md | player behavior / muxer policy — empirical only (codecs absent from the QTFF spec) | Rebuild the Mediabunny artifacts; streamhash vs source; ffprobe demux; playable-check (expect fail) | UNVERIFIED |
| C61 | Single-layer Dolby Vision (P5/P8) survives `-c copy` into MOV on ffmpeg ≥5.0 with `-tag:v hvc1` (RPU in-bitstream); playback is device/app-dependent; dual-layer P7 needs conversion. | SKILL.md; references/ingest-compatibility.md; references/color-hdr-subs.md; references/verification-safety.md | player behavior — empirical only (DV config boxes absent from the QTFF spec) | ffprobe/dovi_tool DV profile on source vs output per ffmpeg version | UNVERIFIED. AMENDED 2026-07-26 (addendum item 5-5e — the addendum labels this amendment "C26", a digit transposition: C26 is the HDR10-SEI claim and is untouched; the amendment text names Dolby Vision and belongs here): the claim's testable halves are (i) the sample-entry family — `dvh1`/`dvhe` DV-dedicated entries plus cross-compat `hvc2`/`hev2`/`hvc3`/`hev3` — reported by probe.sh's stsd scan (`hevc (dvh1)`, ffprobe codec_tag_string fallback, silent degrade); (ii) the playability split — `dvh1` plays where the `hev1`-family fails, the hvc1-vs-hev1 rule (C27, CONFIRMED) extended to DV entries. Both halves still blocked on real DV artifacts |
| C62 | Atmos (E-AC-3 JOC) and DTS:X object metadata are in-band in the compressed bitstream — preserved by `-c copy` into MOV, always lost on decode to PCM (channel bed only). | references/ingest-compatibility.md; references/codec-landscape.md | player behavior / bitstream — empirical only | mediainfo JOC/DTS:X flags on source vs copied output vs PCM decode | UNVERIFIED |
| C69 | Null-muxer duplicate-DTS complaints in the scrub harness on ms-quantized sources are harness artifacts when gate (d) is clean and the identical lines reproduce on the untouched source — the timeline is then provable by the (d) + strict-mux + --full triple. | scripts/verify.sh (e); references/verification-safety.md | player behavior — empirical only (harness behavior) | ms-timebase fixture: identical line sets source vs output; (d) clean; --full framemd5 match | CONFIRMED 2026-07-26 on the real pair (XLVIII feed.mkv→feed.mov, read-only, T7, ffmpeg 8.1.2): (i) identical LINE SETS — the 152 scrub-harness lines captured under identical accurate seeks at `-threads 1` are byte-identical source vs output after address normalization (all `[null @…] non monotonically increasing dts … X >= X`, muxer-stage; decoder-class 0/0); (ii) gate (d) clean (24090 packets, 0 N/A, strictly monotonic, ms-alternation histogram 15258×990+8832×1020); (iii) decoded identity held by gate (a) packet-hash PASS — bit-identical packets, strictly stronger than the named --full framemd5 match. SYNTHESIS LIMIT recorded in regression test 22: a short x264 ms-fixture mints zero such lines, so the fixture pins the advisory/classification mechanics and the line-set proof lives on the real pair |
| C70 | Decoder complaints (e.g. `mmco: unref short failure`) present in the source at identical timestamps under identical accurate-seek conditions are capture-inherited, not remux-induced; only the source/output delta indicts the pipeline. | scripts/verify.sh (c)/(e); scripts/diagnose.sh | player behavior — empirical only | baseline-subtraction run on the XLVI pair (read-only): delta 0 | CONFIRMED 2026-07-26 on the XLVI pair (read-only, T7, ffmpeg 8.1.2) via the mechanized 5-4a/b baseline: identical spot windows — source: 58 / output: 6 / delta: −52; identical off-keyframe accurate seeks at `-threads 1` — decoder-class source: 263 / output: 12 / delta: −251, muxer-stage 0/0/0; gate (d) pristine (74112 packets, 0 N/A, strictly monotonic, exact 37056×1501+37056×1502 histogram). No positive delta anywhere — nothing indicts the remux; the repaired container is in fact QUIETER than the source under identical seeks. The transcript's manual half (identical timestamps on the mmco lines) stands as corroborating session evidence. Verdict: REVIEW-with-evidence, exit 0 — so no 5-4c waiver can or need attach (5-4f outcome: the calibration re-derives this classification mechanically on every run; XLVI's permanent-FAIL memory-dependence is dissolved). Archival sign-off (--full + MKV strict-mux) remains the named open step |
| C64 | ffmpeg's native ALAC encoder accepts 5.1 input (and which layouts it accepts/rejects is enumerable). | scripts/remux.sh (--access-codec, deferred) | player behavior — empirical only (encoder capability) | encode 5.1(side) + 5.1 test tones; record accepted layouts per CI ffmpeg version | CONFIRMED + REFINED 2026-07-26 on ffmpeg 8.1.2: 5.1 accepted — but the encoder never *rejects*; ffmpeg silently negotiates to the NEAREST supported layout: 5.1(side) remaps to 5.1(back), and a 7.1 request lands 6.1(back)/7ch — a silently dropped channel. Any --access-codec alac implementation must pin the layout explicitly and refuse lossy negotiation. Per-CI-version pin owed; C65 still blocks 5-2e |
| C65 | ALAC 5.1 in MOV carries a `chan` layout QuickTime maps to the correct speakers (the C31 bar: per-channel ident tones land on the right speakers). | scripts/remux.sh (--access-codec, deferred); references/dual-track-quicktime.md | markdown/media_data_atom_types/sound_media/sound_sample_descriptions.md; audio_channel_layout_atom | `mp4dump` stsd+chan on an ALAC-5.1 build; speaker-ident listen on the target Mac | UNVERIFIED (blocks 5-2e; listen session is a Round-5 carry-forward) |
| C66 | In a MOV carrying stereo + 5.1 as separate enabled tracks (not the dual-track access pattern), QuickTime's default-track selection and alternate reachability follow tkhd enabled flags + alternate_group as written by ffmpeg for this case. | scripts/mov.sh (layouts policy) | markdown/track_atoms/track_header_atom.md; alternate groups | `mp4dump` tkhd/alternate_group on a layouts-policy output; GUI check which plays, whether the other is selectable | UNVERIFIED — structure half probed 2026-07-26 on ffmpeg 8.1.2 (mp4dump --verbosity 2, layouts multi output): video enabled=1 group=0; a:0 enabled=1 group=1; a:1 enabled=0 group=1 — ffmpeg writes proper QTFF alternate-group semantics with only the first audio enabled, no explicit dispositions needed. GUI half (menu offers/switches the alternate) awaits the listen session |
| C67 | Hierarchical-B streams legitimately carry max(PTS−DTS) = reorder_depth × frame_duration; a fixed one-frame lead bound structurally rejects valid ≥2-level pyramids. | scripts/pairfill-paff.sh; scripts/lib-paff.sh | markdown/sample_atoms/composition_offset_atom.md | measure offset cycle on the XLVI deliverable (read-only, T7) + synthesized b-pyramid fixture; assert derived bound admits both | CONFIRMED 2026-07-26 on ffmpeg 8.1.2, both probe halves: (i) XLVI live measurement (read-only, T7) — source pair-ramp PTS excursion +9009 = exactly 3.00 pairs (min −6006), preroll 27027, deliverable max(PTS−DTS) 36036 = preroll + excursion; the fixed bound (30030) refused this legitimate 3-level pyramid, the derived bound (39039) admits it. (ii) Synthesized frame-coded hierarchical-B fixture (x264 bf=8 b-pyramid, regression test 22): excursion +21021 = 3.50 pairs, output max offset 54054 > old fixed limit 36036, admitted at derived 57057, gates green, presentation order framemd5-identical to source. Wrong-cadence ramp stays refused (test 19 pin). Field-coded pyramid fixture remains blocked on 5h's real capture |
| C68 | MKV's 1/1000 timebase survives MKV→MOV as a coarse video timescale (e.g. 1/16000) with alternating sample durations; the alternation is source rounding (±0.5 ms), not judder, and `-video_track_timescale` is a conventionality fix that must not be escalated to a restamp on reorder streams. | references/timeline-repair.md (beside C02); scripts/probe.sh; scripts/remux.sh | markdown/media_atoms/media_header_atom.md; time-to-sample_atom | ms-timebase MKV fixture → remux with/without --timescale; stts histograms both sides; framemd5 identity | CONFIRMED 2026-07-26 on ffmpeg 8.1.2 (regression test 22 + real pair): fixture 23.976p MKV (1/1000) → default remux inherits timescale 1/16000 with alternating 672/656-tick durations (the source's own 42/41 ms rounding); `--timescale 24000` re-expresses the SAME durations as 1008/984 ticks with video packets bit-identical (streamhash ⊇ framemd5) — the alternation lives in the timestamps, not the base. Real pair (XLVIII, T7 read-only): 1/1000 → 1/30000 with 15258×990+8832×1020 (33/34 ms) — the claim's "e.g." values live. Restamp-prohibition half is doctrine (timeline-repair.md §MKV millisecond timebase; probe.sh advisory prints it), enforced by rebuild-paff's reorder refusal |
| C63 | The set of codecs AVFoundation decodes is a function of macOS version; a playable-check result is valid only for the OS it ran on, and OS upgrades can revoke playability of previously-passing files (Tahoe 26.4: Motion JPEG variants, AIC). | references/ingest-compatibility.md; scripts/playable-check.sh; scripts/probe.sh (vnative `variant` arm); scripts/mov.sh (measured-drop warning) | player behavior — empirical only | MJPEG-MOV fixture: playable-check on Darwin 25.x; re-run after next macOS update; AIC leg blocked on artifact | UNVERIFIED — first-party data point recorded 2026-07-26 on macOS 26.5.2/Darwin 25.5 (already post-Tahoe-26.4, so this is post-drop data): ffmpeg mjpeg-in-MOV **yuvj420p renders** (playable-check OK) while **yuvj422p renders NO frame and hangs qlmanage** (no thumbnail ≥2 min) — the variant-scoped drop reproduced on the AVFoundation stack, matching the forum reports' scope. SECOND first-party point 2026-08-14 on macOS 26.6.1/ffmpeg 9.0.1 (WO 5.1): yuvj420p again renders (playable-check OK, F8 mjpeg.mov via mov.sh DONE) while yuvj422p again renders NO frame (bounded qlmanage stall, killed at the WO 1.4 deadline → verdict=fail → honest REVIEW) — the variant drop PERSISTS on 26.6.1 even as C72 restored MPEG-2/H.264/HEVC 4:2:2 on the same bench; now regression-pinned verdict-following (drift-proof) in tests/regression.d/51-native-matrix.sh. Full confirmation still needs: pre-26.4 comparison, QuickTime-Player GUI check of the 422 file, and the AIC leg (no ffmpeg encoder — blocked-on-artifact). Operational note: on dropped variants playable-check may STALL rather than fail fast (qlmanage hang — now bounded, WO 1.4) |
| C71 | A legacy `gama` atom (absent nclc/colr) changes QuickTime's rendering of the same essence versus an nclc-tagged copy; ingest should surface it. | references/color-hdr-subs.md; scripts/probe.sh | markdown/video_media/video_sample_description_extensions.md (+ color_parameter_atom conflict note) | inject gama via mp4edit on a fixture; A/B in QuickTime Player | UNVERIFIED — detection half probed 2026-07-26 (ffmpeg 8.1.2, Bento4): `gama` (fixed-point 2.2) injected via `mp4edit --insert moov/…/stsd/avc1` lands beside `pasp` in the sample entry (`[gama] size=8+4`); probe.sh reads `PR_GAMA=yes` and prints the ingest WARN; a `-c copy` remux of the injected fixture carries ZERO gama atoms — the normalization claim in the WARN text holds. The rendering half (A/B of injected vs clean in QuickTime Player) is the row's deciding probe and awaits the GUI session |
| C72 | AVFoundation 4:2:2/high-chroma decode support is a function of macOS version — and on macOS 26.6.1 every formerly-refused contribution class decodes (MPEG-2 4:2:2; H.264 High 4:2:2, 8- and 10-bit; HEVC Rext 4:2:2 10-bit; plus 10-bit 4:4:4) — so any hardcoded "cannot decode" codec gate rots, and QuickTime playability of these profiles must be proven per file, post-build, on the target macOS. | scripts/lib-paff.sh (contribution_advisory + playability_verdict); scripts/mov.sh; scripts/auto.sh; SKILL.md (backhaul row + hard-won facts); ../mov/SKILL.md | player behavior — empirical only | mov.sh on each F5 fixture (m2v422.mov/.ts, h264_422.ts, h264_422_10.ts, hevc_422_10.mov) → `MOV_PLAYABILITY` verdict; independent avconvert whole-file decode; re-run after every macOS update | CONFIRMED 2026-08-13 on macOS 26.6.1 / ffmpeg 8.1.2, by BOTH methods: qlmanage thumbnail (playable-check.sh, the AVFoundation stack) AND avconvert whole-file decode (50/50 frames) on every listed class — falsifying the 2026-07-30/31 categorical "cannot decode" (C56 4:2:2 half REVERSED; the 26.5.2-era failures stay recorded there). The old gate was also under-broad: its exact `yuv422p` match never announced the 10-bit profiles at all. Caveat: the falsifying clips were synthetic — they disprove the categorical claim, not every real broadcast master; hence the per-file post-build check rather than a new hardcoded "can decode" claim. Regression pin: tests/regression.d/41-422-empirical.sh (suite re-validated on ffmpeg 9.0.1, 2026-08-14). **NARROWED 2026-08-15 (same bench)**: the caveat materialized — two real 1080i59.94 broadcast masters of the MPEG-2 4:2:2 class rendered DESTROYED (macroblock garbage) on 26.6.1 while the thumbnail check passed them; "decodes" in this row means *renders*, and rendering CORRECTLY is the per-file question C96's fidelity gate answers |

| # | Claim | Stated in | Spec anchor | Probe | Status |
|---|-------|-----------|-------------|-------|--------|
| C73 | The measured QT-native MOV video matrix extends beyond h264/hevc(hvc1)/mpeg2video: MPEG-4 Part 2 (tag `mp4v`), MJPEG 4:2:0 (`jpeg`), DV (`dvcp`) and ProRes (`apcn`) each mux `-c copy` into MOV and fully decode in AVFoundation — while non-4:2:0 MJPEG variants remain undecodable (C63's drop, still in force) and codecs outside the matrix (e.g. ffv1) carry NO assumed verdict. The set is a bench measurement that drifts with macOS (C63/C72, both directions), so recognition self-dates and every unproven class is proven per file, post-build. | scripts/probe.sh (vnative, PR_VNATIVE, advisories); scripts/mov.sh (recognition + WO 4.1 post-build wiring); tests/regression.d/51-native-matrix.sh | player behavior — empirical only | mov.sh on each F8 fixture (mp4v.mov/mjpeg.mov/dv.mov/prores.mov) → DONE + video streamhash identity + playable-check OK; mjpeg-yuvj422p and ffv1 scratch MOVs → build + MOV_PLAYABILITY verdict decides the exit (ok→0, fail/skip→10); re-run after every macOS update | CONFIRMED 2026-08-14 on macOS 26.6.1 / ffmpeg 9.0.1: all four F8 classes built DONE with bit-exact video (streamhash-identical, tags mp4v/jpeg/dvcp/apcn preserved across copy) and passed playable-check (bounded qlmanage thumbnail, the AVFoundation stack). Negative half, same bench: mjpeg yuvj422p → NO frame (bounded stall → fail → REVIEW); ffv1 → NO frame (fail → REVIEW) — "QuickTime-ready" is earned by measurement, never by codec name. Caveat: synthetic clips (the C72 caveat verbatim); drift insurance = the self-dated annotations + the verdict-following regression pin |

### F. 1.11 sweep — the moved bench (macOS 26.6.1 / ffmpeg 9.0.1)

Rows minted during the 1.11 work order (Phases 1–5) and registered by the 6.2
sweep. Per the bench-move rule above, **every row below was re-run live on the
current bench (macOS 26.6.1 / ffmpeg 9.0.1 / MP4Box-GPAC 26.07 / bash 3.2.57)
on 2026-08-14 by this sweep** — nothing was transcribed from a phase report
unverified; where the sweep's measurement diverged from the reported claim the
row records the divergence (see C80). First-measured dates earlier in the work
order are kept inside each row.

| # | Claim | Stated in | Spec anchor | Probe | Status |
|---|-------|-----------|-------------|-------|--------|
| C74 | ffmpeg 9 removed the long-deprecated `-vsync`: any script passing it dies at option parse ("Unrecognized option 'vsync'"); `-fps_mode` (per-stream, present since 5.1) is the replacement — a toolchain-API fact, the first casualty of the 8.1.2→9.0.1 bench move. | scripts/seam-check.sh (comment at the patch site); bench-move note at the top of this file | toolchain behavior — empirical only (no QTFF page) | `ffmpeg -f lavfi -i testsrc=d=0.1 -vsync 0 -f null -` (expect "Unrecognized option"); same with `-fps_mode passthrough` (runs); `bash tests/regression.d/25-seam-check-ffmpeg9.sh` | CONFIRMED 2026-08-14 on ffmpeg 9.0.1: `-vsync 0` → "Unrecognized option 'vsync'. Error splitting the argument list"; `-fps_mode passthrough` runs clean. seam-check.sh broke exactly here when homebrew moved the bench mid-work-order and was patched in the Phase 2 fix round; regression-pinned (25-seam-check-ffmpeg9.sh) |
| C75 | movenc itself enables exactly ONE audio track (tkhd Enabled on the first mapped audio, Disabled-In-Movie on the rest) and writes `alternate_group=1` on EVERY audio track — `.mov` and `.mp4` modes, single- and multi-audio — so fresh builds are language-menu-conformant (TN3177) straight out of the muxer, no curation; ffmpeg exposes no muxer option for the field. | scripts/qt-groups.sh; references/alternate-group.md ("who needs the post-pass"); SKILL.md | markdown/track_atoms/track_header_atom/flags.md; markdown/track_atoms/track_header_atom/alternate_group.md | fresh 3-audio encode into `.mov` → `MP4Box -info` track flags + Alternate Group; repeat single-audio `.mov` and 2-audio `.mp4`; `ffmpeg -h muxer=mov \| grep -ci alternate` → 0 | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 / MP4Box 26.07 (WO 5.3), re-measured this sweep: 3-audio .mov → flags Enabled/Enabled/Disabled/Disabled (video + first audio only), all three audio traks Alternate Group ID 1; single-audio .mov and 2-audio .mp4 same pattern; muxer help exposes no knob. This is the structure half C40 and C41 lean on — fresh builds never need the retrofit (C76); only foreign/scrubbed files do |
| C76 | The multilang retrofit incantation is ONE MP4Box `-group-add refTrack=0:switchID=-1:trackID=A:trackID=B:…` (trackIDs colon-chained into a single flag): it lands a SHARED nonzero `alternate_group` on the named tracks with essence untouched and moov kept up front; separate `-group-add` args open SEPARATE groups (1,2,3) — the dossier's "incantation unfinished" is finished. | scripts/qt-groups.sh; references/alternate-group.md (avenue log) | markdown/track_atoms/track_header_atom/alternate_group.md | on a group-scrubbed 3-audio .mov: chained `-group-add` → `MP4Box -info` groups all equal; separate args → 1,2,3; full acceptance `bash tests/regression.d/53-qt-groups.sh` | CONFIRMED 2026-08-14 on MP4Box 26.07 (WO 5.3), re-measured this sweep: chained form → all three audio traks share group 1; separate args → groups 1,2,3. qt-groups.sh acceptance re-run green (53-qt-groups.sh 16/16): shared nonzero group, exactly one audio enabled, video `-f md5` + per-stream audio streamhashes unchanged, MP4Box independent parse clean, byte-diff bound proves only the patched 2-byte tkhd fields differ |
| C77 | MP4Box/gpac group-editing hazard matrix (GPAC 26.07): `-group-clean` zeroes tkhd `alternate_group` on every trak, essence intact (the group-scrubbing hazard class and the retrofit-fixture mint); `-add self#N:group=G` DOES land the group but wrong-scoped — it re-imports and breaks uniform grouping ({1,1,1}→{5,1,1}), correcting the dossier's "group does not land"; a gpac filter session (`gpac -i in.mov -o out.mov`) silently DROPS disabled tracks (4→2 measured on a 3-audio build) unless `:alltk` is given; plain in-place edits (measured: `-lang 2=eng`) PRESERVE existing groups — the draft "any MP4Box edit strips groups" is UNREPRODUCED on this bench. | references/alternate-group.md (avenue log); scripts/qt-groups.sh (why it patches bytes) | markdown/track_atoms/track_header_atom/alternate_group.md | the four commands above on a movenc 3-audio build + `MP4Box -info` group/track-count/enabled diff each way | CONFIRMED 2026-08-14 on MP4Box/GPAC 26.07 (WO 5.3), all four legs re-measured this sweep: `-group-clean` → zero Alternate Group lines; `-add self#2:group=5` → {5,1,1}; gpac session 4→2 tracks, `:alltk` keeps 4; `-lang 2=eng` in place → groups {1,1,1} and the enabled set intact |
| C78 | VP9 cannot be muxed into MOV: movenc hard-refuses at header write ("vp9 only supported in MP4") while the same `-c copy` into MP4 succeeds (tag `vp09`) — write-side muxer policy, the C60 family, and the end-to-end proof body for the unroutable-vcodec gate. | tests/make-fixtures.sh (fx_vp9 self-check); scripts/probe.sh (unroutable advisory) | muxer policy — empirical only | `ffmpeg -i tests/fixtures/vp9.webm -c copy out.mov` (expect header-write fail, exit ≠0) vs same into `.mp4` (succeeds, tag vp09); make-fixtures.sh re-checks the impossibility on every mint and fails loudly if a future ffmpeg learns VP9-in-MOV | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (WO 5.2), re-measured this sweep: MOV mux exit 234, "[mov] vp9 only supported in MP4" + "Could not write header", 0-byte output; MP4 route succeeds (vp09) |
| C79 | Unroutable classes are refused PRE-FLIGHT since 1.11 (WO 5.2): VC-1/VP9/AV1 video → exit 11 + `MOV_REFUSED profile=unroutable-vcodec vcodec=<c>`; Dolby E audio (`dolby_e`, any manifest track, honoring an excluding `--audio-keep`) → exit 11 + `MOV_REFUSED profile=dolby-e-audio track=a:<N>`, naming the routes (specialist decode — ffmpeg's dolby_e decoder → WAV — is operator-invoked, never automatic; probe.sh `PR_AUDIO_ACTION=specialist` is the additive signal). These are MOV_REFUSED's ONLY emitters; the 1.8.0–1.10.0 backhaul profiles (`qt-undecodable-*`, rot) stay retired. NAMED LIMITATION: Dolby E hiding inside a PCM track (SMPTE 337M/AES3 wrapping) is NOT auto-detected — a broadcast "PCM" track playing as steady full-scale noise is the signature. | scripts/mov.sh; scripts/probe.sh; SKILL.md (decision-table row + MOV_REFUSED API row) | player/muxer behavior — empirical only | `bash tests/regression.d/52-unroutable-codecs.sh` (27 assertions: refusal exits, MOV_REFUSED grammar, --audio-keep exclusion, advisory honesty); VP9 leg raw per C78 | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (WO 5.2), suite re-run 27/27 this sweep. The refusal is muxer/decoder physics (VP9: C78; VC-1/AV1: no QT decode and no attested lossless route), not curation — consistent with the plugin's end-goal rule |
| C80 | Blu-ray/DVD authoring PCM does not survive into a playable MOV, and the two classes fail DIFFERENTLY (ffmpeg 9.0.1): `pcm_bluray` `-c copy` MUXES — movenc writes the HDMV codec_tag `[128][0][0][0]` (0x0080) — producing a dead track NOTHING decodes back (ffprobe codec_name=unknown; ffmpeg itself: "no decoder found"); `pcm_dvd` is REFUSED at header write ("Could not find tag for codec pcm_dvd … not currently supported in container"). Either way the only QT-playable route is decode to PCM access audio — the WO 3.1 routing. | scripts/mov.sh + scripts/auto.sh (per-track routing); scripts/verify.sh gate (g) (dead-track catch) | markdown/media_data_atom_types/sound_media/sound_sample_descriptions.md (no HDMV fourcc in the registry) | `ffmpeg -i tests/fixtures/pcm_bluray.m2ts -map 0:a -c copy x.mov` → tag 0x0080, codec_name=unknown, decode-back fails; `ffmpeg -f lavfi -i sine=d=1 -c:a pcm_dvd -f vob p.vob` then `-c copy` to .mov → header-write refusal; `bash tests/regression.d/31-pcm-bluray.sh` | CONFIRMED 2026-08-14 on ffmpeg 9.0.1, both halves measured this sweep (31-pcm-bluray.sh 21/21). **Divergence recorded:** the Phase-3 report's "MOV muxer accepts pcm_bluray/pcm_dvd" was measured HALF-wrong — pcm_dvd never muxes on this bench (movenc has no tag for it); only pcm_bluray produces the dead HDMV track class that gate (g) exists to catch |
| C81 | ADTS AAC from TS converts to MP4A/ASC automatically on `-c copy` into MOV — ffmpeg inserts `aac_adtstoasc` itself; no route needs a manual `-bsf`. ffmpeg 9.x quirk: the "Automatically inserted bitstream filter" notice prints only at `-v verbose` (8.x printed it at default level) — a silent mux log does NOT mean the filter didn't run. | references/known-limits.md ("the bitstream filter is automatic"); scripts (no copy route passes a manual -bsf) | toolchain behavior — empirical only | `ffmpeg -v verbose -i tests/fixtures/aac.ts -c copy out.mov 2>&1 \| grep 'Automatically inserted'` (hit); same at default `-v` (zero hits); ffprobe → mp4a/LC; `-i out.mov -f null -` clean | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (previously 2026-08-13 on 8.1.2), re-measured this sweep: source syncword 0xFFF1; verbose log names `aac_adtstoasc`; default log silent (quirk measured); output mp4a/LC, decodes clean. Verified non-issue |
| C82 | dual-track.sh `--ss/--to` are relative to the container's `start_time`, NOT absolute PTS — on a start_time≈1372.69 capture, the defect observed at 1374.27 is cut with `--ss 1.58`; passing the absolute value seeks past EOF (the WO 1.3 guard names the origin: "cut produced no video … --ss is relative to start_time"). | scripts/dual-track.sh (header ORIGIN note + guard) | toolchain behavior — empirical only (ffmpeg input-seek origin) | `dual-track.sh tests/fixtures/mixed.ts out.mov --ss 2` on the start_time=1.459/5.03 s fixture → ≈3.3 s output (start-relative, keyframe-bound), not the ≈4.5 s an absolute-PTS origin would leave | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (first measured WO 1.3), re-measured this sweep: full build 5.034 s; `--ss 2` build 3.328 s — start-relative cut snapped to the prior keyframe; the absolute-PTS interpretation (≈4.5 s remaining) is excluded by the measurement |
| C83 | ffmpeg's default probe window (5 MB / 5 s) is structurally defeated by high-bitrate TS captures starting mid-GOP with late SPS/PPS: the demuxer reports the video stream DIMENSION-BLIND (h264 0x0, "non-existing PPS") and every downstream mapping/gate decision is built on the blind read — why lib-probe.sh defaults `RTM_PROBESIZE`/`RTM_ANALYZEDURATION` to 200M for every consumer. | scripts/lib-probe.sh (header + FF_INPUT_OPTS); tests/fixtures/late-sps.ts | toolchain behavior — empirical only | `ffprobe tests/fixtures/late-sps.ts` → h264 0x0 + PPS errors; `ffprobe -probesize 500M -analyzeduration 500M` → 1280x720; `bash tests/regression.d/11-probe-defaults.sh` | CONFIRMED 2026-08-13 on ffmpeg 8.1.2 (WO 1.1 — late-sps.ts minted as the falsifier), re-verified 2026-08-14 on 9.0.1 this sweep: default probe h264,0x0; widened probe 1280x720. Regression-pinned (11-probe-defaults.sh) |
| C84 | macOS bash 3.2.57: under `set -e`, ANY EXIT trap observes `$?=0` from a `${1:?}` usage abort or a `set -u` unbound-variable death — the script "succeeds" while dying — so exit-code-contract cleanup must ride an ERR trap, which fires with the TRUE failing status. Why lib-exit.sh installs an ERR trap, deliberately never an EXIT trap. | scripts/lib-exit.sh (MECHANISM block) | shell behavior — empirical only | three 3-line scripts: `set -e; trap true EXIT; : ${1:?}` → exit 0; `set -eu; trap true EXIT; echo $UNSET` → exit 0; ERR-trap form → exit 1 | CONFIRMED 2026-08-14 on /bin/bash 3.2.57(1) (first measured during item 1.4), re-measured this sweep: EXIT-trap `${1:?}` death exit 0, EXIT-trap `set -u` death exit 0, ERR-trap form exit 1 |
| C85 | bash 3.2 parser trap: an apostrophe inside an UNQUOTED `${1:?can't …}` message opens a single-quote that eats source lines up to the next apostrophe-plus-closing-brace — when a later line supplies both (e.g. a second `${n:?…}` guard whose message also carries an apostrophe), the intervening lines are swallowed SILENTLY: no error, exit 0, the swallowed commands never ran. With no later closer it at least dies loudly (unexpected EOF, exit 2). House rule: `${n:?}` messages are double-quoted or apostrophe-free. | scripts house style (measured in item 2.2) | shell behavior — empirical only | 5-line script: `: ${1:?can't run}` / two echoes / `: ${2:?doesn't fire}` / `echo REACHED_END` — only REACHED_END prints, exit 0 | CONFIRMED 2026-08-14 on /bin/bash 3.2.57(1), both modes measured this sweep: silent swallow (REACHED_END only, exit 0) and the loud-EOF form (exit 2) when no closer exists |
| C86 | `qlmanage -t` on input AVFoundation cannot decode does not fail — it HANGS (no thumbnail, no exit; ≥2 min first measured on the C63 MJPEG-4:2:2 file) — so an unbounded empirical render probe wedges the pipeline; playable-check.sh gives the render a deadline (`RTM_QL_TIMEOUT`, default 60 s — a healthy thumbnail lands in single-digit seconds) and a deadline kill is `verdict=fail`, an honest bounded answer. | scripts/playable-check.sh (QLLIMIT + WHY block) | player behavior — empirical only | playable-check.sh on an mjpeg-yuvj422p or ffv1 MOV → bounded kill at the deadline, verdict fail, pipeline continues; exercised via `bash tests/regression.d/51-native-matrix.sh` | CONFIRMED 2026-08-14 on macOS 26.6.1 (bound built in WO 1.4; hang first observed 2026-07-26 on the C63 file): this sweep exercised both stall classes through 51-native-matrix.sh (60/60) — deadline kills fired, verdicts fail, honest REVIEW exits; healthy classes thumbnail in seconds |
| C87 | Damage detection REQUIRES the transport pass: TEI-flagged TS packets are dropped by the demuxer and NEVER reach the decoder, so a decode-error tally cannot see real damage (under-detection — corrupt.ts earned NO damage verdict at pre-2.1 HEAD) — while pre-roll decode noise on a mid-GOP capture start with ZERO transport counters is NOT damage (the 2026-08-13 false-SOURCE-DAMAGED finding; over-detection). ts-health.sh therefore scans transport (CC/TEI/PES) and timeline separately, and routes mid-GOP pre-roll to the IDR trim, never a damage verdict. | scripts/ts-health.sh; scripts/diagnose.sh (WO 2.1) | toolchain behavior — empirical only | `ts-health.sh tests/fixtures/late-sps.ts` → transport all zero + mid-GOP trim finding (no damage); `ts-health.sh tests/fixtures/corrupt.ts` → TEI/PES counters drive the transport finding | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (first measured WO 2.1, 2026-08-13/14), both fixtures re-run this sweep exactly as stated: late-sps CC=0 TEI=0 PES=0 with 102 pre-keyframe packets → trim advisory; corrupt TEI=14 PES=2 → "PERMANENT but small" transport finding + the source==output decode-noise expectation verify's baseline classification consumes |
| C88 | ffmpeg streamcopy silently DROPS every video packet before the first keyframe (`-copyinkf`, an OUTPUT option, defaults off): a mid-GOP TS start loses its pre-roll with no warning line, and the resulting source-vs-output packet/duration delta masquerades as a gap-collapse desync — the phantom 4.25 s REVIEW root cause. mov.sh therefore auto-trims at the first IDR via trim-to-idr.sh so the cut is DECLARED, gop-probe-proven, and applied to BOTH tracks in step. | scripts/mov.sh (auto-trim wiring); scripts/trim-to-idr.sh | toolchain behavior — empirical only | late-sps.ts: source 477 video packets → `-c copy` output 375 (102 silently dropped; only demux-side PPS noise in the log); `-copyinkf` on the output side keeps 477; `bash tests/regression.d/22-trim-to-idr.sh` | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (first measured WO 2.2/2.3), counts re-measured this sweep: 477→375 with zero drop warnings; `-copyinkf` restores 477. Regression-pinned (22-trim-to-idr.sh) |
| C89 | movenc's audio sample-entry tag case is a fresh-vs-copy split: FRESH encodes write lowercase `ac-3`/`ec-3`; stream COPIES preserve the demuxer's uppercase `AC-3`; fresh DTS writes `dtsc`; MP2 lands `.mp2` fresh or copied — so verify's gate (g) dead-track allowlist carries BOTH cases of ac-3/ec-3 plus `dtsc`/`.mp2`, the legal preserved-original tags on dual-track builds. | scripts/verify.sh (gate (g) allowlist + comment) | muxer behavior — empirical only (tags outside the QTFF sound registry per C32/C55) | fresh: `-f lavfi -i sine -c:a ac3\|eac3\|dca\|mp2` into .mov → ffprobe codec_tag_string; copy: `-i tests/fixtures/mixed.ts -map 0:a:1 -c copy` → uppercase AC-3 | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (Phase 3 first measured), all five legs re-measured this sweep: fresh ac-3 / ec-3 / dtsc / .mp2; TS copy preserves uppercase AC-3; MP2 copy .mp2 |
| C90 | Multi-program TS: `-map 0:v:0` (every route) selects the FIRST video in ffmpeg's demux order = PMT parse order = the first program in the PAT — declared PAT order decides (reversing it flips which resolution wins), and the other programs' video is never mapped — why probe.sh prints the program count when >1. (Silence half SUPERSEDED in the 1.11 fix round: remux.sh's non-audio census now WARNs the unmapped video + `RMX_PLAN unmapped=` — known-limits.md, multi-program section; the winner rule itself stands.) The per-track audio manifest enumerates `-select_streams a` across the WHOLE file (all programs, index-deduped), so `--audio-keep all` keeps every program's audio with correct PMT language tags. | scripts/probe.sh (program NOTE); references/known-limits.md (winner rule + isolation route + repro) | container behavior — empirical only (PAT/PMT are MPEG-TS, outside QTFF) | build the 2-program fixture in BOTH PAT orders (known-limits.md repro), `-map 0:v:0 -c copy` each → winner resolution flips; `ffprobe -select_streams a` shows all programs' audio + language tags; probe.sh prints the NOTE | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (WO 5.4), fixtures re-built + re-measured this sweep: ProgA-first PAT → 320x240 wins, reversed PAT → 640x480 wins; audio enumerates both programs (eng+spa) in both orders; probe.sh 2-program NOTE fires naming the rule |
| C91 | A mid-stream SPS/resolution change (broadcast splice) `-c copy`-muxes into MOV with NO warning; the stsd sample description declares the FIRST resolution for the whole track while the samples decode at BOTH sizes — probe/verify/playable-check all pass, and QuickTime's rendering past the splice is UNVERIFIED. Detect-and-warn candidate, not implemented. | references/known-limits.md (broadcast-splice section) | markdown/media_data_atom_types/video_media/video_sample_description.md (one description per stsd entry) — behavior half empirical | two mpeg2video ES clips at 320x240/640x480, `cat`, `-c copy` into .mov; ffprobe stream WxH vs `-show_frames` width,height histogram | CONFIRMED (structure) 2026-08-14 on ffmpeg 9.0.1 (WO 5.4), re-built this sweep: mux exit 0, zero warn/error lines; stream declares 320x240 for all 100 samples; frames decode 50×320x240 + 50×640x480. The QuickTime-past-the-splice half is the row's owed probe |
| C92 | A standalone EIA-608 stream CAN ride MOV on ffmpeg 9.0.1: `-c:s copy` writes a native `c608` closed-caption track that round-trips back to text (`-map 0:s:0 out.srt` recovers the caption); the MP4 muxer refuses the identical copy ("Could not find tag for codec eia_608 … not currently supported in container") — the historical mux error is MP4-specific. No plugin route maps subtitle/caption streams: a c608-track source remuxes video-only (SILENT-drop half SUPERSEDED in the 1.11 fix round — remux.sh's non-audio census WARNs the drop + `RMX_PLAN unmapped=`; the no-route-maps-it half stands, manual carry documented in known-limits.md). | references/known-limits.md (captions section) | markdown/media_data_atom_types/closed_captioning_media.md | SCC fixture (`Scenarist_SCC V1.0`, pop-on caption) → `-f scc -i cap.scc -c:s copy x.mov` → c608 tag + srt round-trip; same into .mp4 → refusal message | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (WO 5.4), re-measured this sweep: MOV mux OK (eia_608/`c608`), round-trip srt carries the caption text ("Hello world!" fixture); MP4 refuses with the exact message. Complements C35 (in-bitstream A/53 captions), which stays UNVERIFIED |
| C93 | The 33-bit PTS horizon is 2^33/90000 ≈ 95443.7 s ≈ 26.5 h; ffmpeg unwraps ONE rollover on read — a constructed mid-file wrap reads back as a single continuous timeline, so single-wrap captures remux clean — while ≥2 wraps (~53 h) have ambiguous epochs and break: DOCUMENTED-NOT-MEASURED (derived from field width; no 53 h fixture exists). probe.sh advises at duration >24 h (approaching the horizon); ts-health.sh's `TSH_WRAP` counts only residual raw backward jumps the demuxer did NOT unwrap. | scripts/probe.sh (>24 h NOTE + comment); scripts/ts-health.sh (TSH_WRAP); references/known-limits.md (wraparound section) | container behavior — empirical only (the 33-bit PTS width is MPEG-TS) | `-output_ts_offset 95442` on a 4 s TS fixture → packet timeline reads continuous across the boundary (measured −1.70 s → +3.22 s, no 26.5 h discontinuity); advisory: a setts-scaled long-duration MKV → probe.sh NOTE fires with the horizon text | CONFIRMED (measured halves) 2026-08-14 on ffmpeg 9.0.1 (WO 5.4), re-measured this sweep: one-rollover unwrap live on the constructed wrap fixture (ts-health sees the unwrapped timeline — wraparounds=0 — confirming the unwrap happens upstream of any scan); the >24 h NOTE fired verbatim on a 70.4 h-reading fixture. The ≥2-wrap break stays documented-not-measured, named in probe's advisory |

| C94 | The automatic ADTS→ASC reframing (C81) changes the PACKET-level streamhash of a bit-identical AAC payload: `-c copy -f streamhash` of a TS/ADTS AAC track ≠ the same hash of its MOV-copied ASC form, while the decoded payloads are md5-identical — so a raw packet-hash compare of source-vs-preserved-original falsely reads "corrupted" on every TS-AAC dual-track build. verify.sh `--audio` therefore re-hashes the SOURCE through `aac_adtstoasc` before judging (1.11 fix round; the false rc=1 FAIL on `mov.sh --always-dual tests/fixtures/aac.ts` was the adversarial-review finding). AAC-only: ac3/eac3/dts/mp2 copy frame-for-frame and keep the strict raw-hash gate. | scripts/verify.sh (--audio reframing fallback); references/known-limits.md (ADTS corollary); references/verification-safety.md (checklist row) | toolchain behavior — empirical only (C81 corollary) | `ffmpeg -i tests/fixtures/aac.ts -map 0:a:0 -c copy -f streamhash -hash md5 -` vs the same `+ -bsf:a aac_adtstoasc` (hashes differ→match the MOV copy); `bash tests/regression.d/60-fix-round.sh` section 2 (DONE + wrong-source negative control) | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (1.11 fix round): raw hashes differ, reframed source hash == output a:1 hash, decoded payloads md5-identical; mov.sh --always-dual aac.ts now exits 0 DONE; mismatched-source control still FAILs |
| C95 | A lossless `-c copy` MKV of a clean H.264 source legitimately reports N/A DTS on B-frame head packets (`N/A-DTS=2` on the aac.ts copy), so verify.sh gate (d)'s zero-N/A demand — a QTFF-shaped assertion — FAILs a bit-identical matroska cross-check output (rc=1). Pre-existing at 1.10.0, unchanged by the 1.11 gate reform; the honest route is `ts-health.sh` for MKV cross-checks (documented, not code-changed — a matroska exemption is the recorded candidate shape). | references/known-limits.md (MKV cross-check section); references/verification-safety.md (container-scope note) | container behavior — empirical only (matroska block timing vs QTFF stts/ctts) | `ffmpeg -i tests/fixtures/aac.ts -map 0:v:0 -map '0:a?' -c copy xc.mkv && bash scripts/verify.sh tests/fixtures/aac.ts xc.mkv; echo rc=$?` → gate (d) `N/A-DTS=2`, rc=1 | CONFIRMED 2026-08-14 on ffmpeg 9.0.1 (1.11 adversarial review): packets=125 N/A-PTS=0 N/A-DTS=2 → FAIL on a bit-identical copy; documented as a named limit, gate unchanged |

### G. 1.12 round — the fidelity storey (macOS 26.6.1 / ffmpeg 9.0.1)

Rows minted during the 1.12 work order. Per the bench-move rule above, every
row below was measured live on the current bench (macOS 26.6.1 / ffmpeg 9.0.1)
on 2026-08-15; where a leg could not run on this bench the row says so instead
of transcribing a claim unverified.

| # | Claim | Stated in | Spec anchor | Probe | Status |
|---|-------|-----------|-------------|-------|--------|
| C96 | A qlmanage thumbnail within the deadline proves a frame RENDERED, not that it rendered CORRECTLY — AVFoundation can decode a class destructively (macroblock garbage) while still producing a thumbnail — so a thumbnail-only playable-check can false-green a broken build, and QuickTime correctness on contribution profiles is a per-file pixel comparison: the AVFoundation render (bounded `avconvert` ProRes trims, deadline-killed) vs the ffmpeg reference decode, SSIM after yuv444p chroma normalization, worst sample ≥ `RTM_FIDELITY_SSIM` (default 0.90). | scripts/playable-check.sh (`--fidelity` + PLAYCHECK_FIDELITY); scripts/lib-paff.sh (`fidelity=` field); scripts/mov.sh + scripts/auto.sh (contribution-profile wiring); SKILL.md; references/ingest-compatibility.md | player behavior — empirical only | the two real `m2v1` 1080i59.94 masters that verdict=ok'd while rendering garbage → `--fidelity` must FAIL them; healthy synthetic tests/fixtures/m2v422.mov → `--fidelity` OK; threshold-forced run (`RTM_FIDELITY_SSIM=0.99`) → `verdict=fail reason=fidelity` exit 1; re-run after every macOS update | CONFIRMED (mechanism + healthy leg) 2026-08-15 on macOS 26.6.1 / ffmpeg 9.0.1: the false-green itself was measured on the two real masters (playable-check OK, QuickTime render destroyed — the motivating incident); the gate built the same day measures the healthy fixture at worst-sample SSIM 0.9541 ≥ 0.90 (Y 0.9993 — the residual depression is chroma-siting in the AVFoundation ProRes path; a PNG/RGB reference detour scores 0.9268 on the same pair, which is why the reference frame stays native-YUV FFV1/NUT) while destroyed-render stand-ins score 0.8008 (wrong-time content) and 0.5719 (simulated macroblock garbage) — the default threshold separates. Method fact, same bench: an infinite `-loop 1` secondary input hangs the ssim graph; a 1-frame secondary + framesync repeatlast terminates and pairs the reference against every rendered frame (alignment-free best-match). OWED: the broken-master leg through `--fidelity` (the masters live off-repo); until it runs, the fail half rests on the threshold-forced probe. Regression pin: tests/regression.d/43-fidelity-retag.sh (bench-split per Ground Rule 6 — off-macOS the un-provable halves are asserted as announced SKIPs, never pretended green) |
| C97 | stsd FourCC decoder dispatch, third instance: MPEG-2 4:2:2 tagged `m2v1` routes to AVFoundation's consumer MPEG-2 decoder (macroblock garbage on two real 1080i59.94 broadcast masters) while the SAME bitstream tagged `xd5b` routes to the pro XDCAM HD422 decoder (frame-for-frame = the ffmpeg reference); the retag is lossless (`-c copy -tag:v xd5b`, one FourCC, zero payload bytes) and CBR is not enforced (19.7/31.2 Mb/s VBR masters played). | SKILL.md (instant answers); references/ingest-compatibility.md (`xd5*` table); references/known-limits.md (deferral record); scripts/probe.sh (`PR_TAG_ADVICE`) | player behavior — empirical only (which decoder AVFoundation binds to an stsd FourCC is outside the QTFF spec; the sample-description page is markdown/media_data_atom_types/video_media/video_sample_description.md) | the two real masters retagged A/B (`m2v1` vs `xd5b`); per-file `--fidelity` on each build | CONFIRMED 2026-08-15 on macOS 26.6.1 / ffmpeg 9.0.1 on the two real broadcast masters; the synthetic fixture decodes clean through BOTH tags — the corruption needs real broadcast content, so the synthetic leg cannot falsify the dispatch. Regression pin: tests/regression.d/43-fidelity-retag.sh pins the advisory + lossless-retag halves |
| C98 | movenc movie-timescale overflow family: the `duration too long for timebase` warning fires at 2^31 movie-timescale ticks (bracketed 2982/2983 s at 720000; LCM-dependent — 1048/1049 s at 2048000) and `-video_track_timescale` cannot move it (600/30000/90000 all tested); in-band the warning is benign as measured (ffmpeg writes 64-bit version-1 atoms file-wide; decode/scrub/chapter menu verified); past 2^32 TOTAL ticks the QT chapter-menu track is geometry-gated silently DROPPED iff the FIRST chapter spans >2^31 ticks — and the warning is SUPPRESSED whenever any single chapter spans >2^31 ticks, so the loss is silent; `Referenced QT chapter track not found` on open is the detection signature. | references/known-limits.md; references/timeline-repair.md; scripts/mov.sh; scripts/resync.sh (two-tier pre-announce + `MOV_CHAPTER_TS_WARN`) | movenc behavior — empirical only (the 2^31/2^32 boundaries are ffmpeg implementation, not QTFF spec) | the `-t` duration sweep bracketing the warning onset + the chapter-geometry matrix (first-chapter vs total-tick quadrants); re-run after every ffmpeg upgrade | CONFIRMED 2026-08-15 on macOS 26.6.1 / ffmpeg 9.0.1, on two independent rigs |

### H. 1.13 round — the container axis (macOS 26.6.1 / ffmpeg 9.0.1)

Rows minted during the 1.13 defect round, 2026-08-15. Two of them **narrow a
row from section G on the day it shipped** — that is what this registry is for.

| # | Claim | Stated in | Spec anchor | Probe | Status |
|---|-------|-----------|-------------|-------|--------|
| C99 | The MPEG-2 4:2:2 QuickTime-corruption class has a CONTAINER axis, not only a tag axis: the same bitstream renders destroyed from a `.mov` (`m2v1`+`glbl`) and correctly from a `.mp4` (`mp4v`+`esds`). Corollary that narrows C97: a MOV retag cannot change what AVFoundation is fed, because movenc writes the IDENTICAL generic sample-description body for every MPEG-2 fourcc (`glbl`(extradata)+`fiel`+optional `colr`; movenc.c 1926–1986, 2756–2784, 2946) — the tag changes the FourCC and the compressor-name string, nothing else. | references/ingest-compatibility.md ("The container axis"); references/known-limits.md; scripts/mp4-swap.sh; scripts/remux.sh (`--container mp4`); SKILL.md | ffmpeg source read (libavformat/movenc.c) + player behavior — empirical only | field report 2026-08-15: 21 GB MPEG-2 4:2:2 1080i29.97 capture — five MOV retags (`m2v1`/`mp2v`/`hdv3`/`xd5b`/`xd5c`) vs one MP4 swap, `playable-check.sh --fidelity` on each | CONFIRMED (operator bench, 2026-08-15, macOS 26.6.1): all five MOV builds SSIM 0.81–0.85 and visually destroyed; the MP4 swap SSIM **0.9175+** on the same three timestamps. The identical-body half is a SOURCE READ, not a measurement. **NARROWS C97**, which claimed the retag fixes the class: it fixes the class only where the stream matches the fourcc's profile contract (the C97 VMA pair did; this capture did not). Not reproducible on the synthetic fixture — the corruption needs real broadcast content — so the regression pin (tests/regression.d/44-container-axis.sh) covers the mechanism halves only: the MOV tag-table refusal, the MP4 entry actually written, and losslessness across the swap |
| C100 | `mp4v`+`esds` is spec-legal INSIDE a `.mov` — Apple's QTFF "video sample description extensions" table lists `esds` as a first-class extension (with `gama`/`fiel`/`mjqt`/`mjht`/`avcC`/`pasp`/`colr`/`clap`) — and ffmpeg's refusal to write it there (`Tag mp4v incompatible with output codec id '2' (m2v1)`) is a MUXER TAG-TABLE artifact: a linear lookup in generic muxer init (libavformat/mux.c) against `ff_codec_movvideo_tags`, which pairs `mp4v` only with MPEG-4, while the MP4 muxer's `codec_mp4_tags` pairs it with MPEG-2. Conversely `glbl` — what the MOV path writes instead — is an FFmpeg invention absent from both the QTFF extensions table and MP4RA's box registry. | references/ingest-compatibility.md; references/known-limits.md ("The MOV/MP4 container axis") | developer.apple.com/documentation/quicktime-file-format/video_sample_description_extensions; mp4ra.org box registry; libavformat/{mux.c,isom_tags.c,movenc.c} | attempt `-tag:v mp4v -f mov` on MPEG-2 (expect the header-write refusal); read the QTFF extensions page and the MP4RA registry | CONFIRMED (refusal + entry) 2026-08-15 on ffmpeg 9.0.1, this bench: MOV refuses at header write; MP4 writes `mp4v`+`esds` **by default** (no explicit tag needed). Spec-legality is a DOCUMENT read, not a measurement. **UNBENCHED and recorded as such:** whether an `mp4v`+`esds` MOV built by `stsd` surgery (MP4Box/Bento4) actually decodes correctly in AVFoundation — nobody has made one. `glbl`-is-inert is a HYPOTHESIS (CoreMedia's documented behavior for unknown stsd atoms is carry-verbatim via `kCMFormatDescriptionExtension_SampleDescriptionExtensionAtoms`), not a measurement |
| C101 | `ipcm` is the ISO-registered PCM sample entry for MP4 (ISO/IEC 23003-5 + the `pcmC` config box, MP4RA-registered), written by ffmpeg since `d4ee177a` (6.1), read by VLC and GPAC (which normalizes QTFF `twos` → `ipcm` on MP4 remux), and shipped by Sony XAVC in broadcast delivery since 2021 — so verify.sh's pre-1.13 rejection text, "a sample entry no decoder claims", was factually false about it. | scripts/verify.sh gate (g); references/ingest-compatibility.md (audio matrix) | mp4ra.org codecs registry; ISO/IEC 23003-5; ffmpeg commits `d4ee177a` (write), `cbe216d3` (pcmC read) | mint `-c:a pcm_s16le -f mp4`; read the sample entry; decode it; run verify.sh gate (g) | CONFIRMED 2026-08-15 on this bench (macOS 26.6.1 / ffmpeg 9.0.1): `pcm_s16le` into MP4 lands as `ipcm`, ffmpeg decodes it clean, and gate (g) now allowlists it. Known holes recorded, not tested here: Android MediaExtractor cannot play it (2025 report), Windows Media Foundation undocumented, Apple ships zero documentation (this bench is the only Apple datapoint), Premiere/Resolve unverified for ffmpeg-authored files. In `.mov`, QTFF PCM (`sowt`/`in24`) stays the compatibility king |
| C102 | Whether AVFoundation decodes MPEG Layer II in `mp4a`/`.mp2` tracks is a PER-OS EMPIRICAL question — asserted categorically 2026-08-15, falsified on this machine 2026-08-29 — and the declaration was never the variable: ffmpeg's `mov_write_esds_tag` special-cases MP2/MP3 above 24 kHz to OTI `0x6B` (MPEG-1 Part 3), the formally correct value for 48 kHz Layer II; the `mp3` label ffprobe returns is a demux-side artifact (`ff_mp4_read_dec_config_descr` maps 0x69 and 0x6B to `AV_CODEC_ID_MP3`, first match — 14496-3 carries no DecoderSpecificInfo for the layer). So an MP2-only QTFF file's audio must be PROVEN on the target machine; the PCM access track is the works-everywhere option, not a mandatory rebuild. | scripts/verify.sh gate (g) (the MP2-without-access-track REVIEW); references/ingest-compatibility.md (audio matrix); SKILL.md | ffmpeg source read (libavformat/{movenc.c,isom.c}) + player behavior — empirical only | build an MP2-only `.mov`; listen on macOS; run verify.sh gate (g) | CONFIRMED (negative) 2026-08-15: measured silent on this bench, and no positive report of Layer II decode in QuickTime X/AVFoundation exists in any container. The OTI half is a source read. Treat MP2 as undecodable by AVFoundation until a counterexample appears. **REVERSED (playability half) 2026-08-29 on this machine**: the counterexample arrived — the feed.ts deliverable (`avc1` video + a `.mp2`-tagged Layer II track, NO PCM access track) plays in QuickTime, audio included (plugin-doctor/README.md, "QuickTime and MP2"). The 2026-08-15 silence measurement STAYS recorded: a registry records reversals, it does not erase them, and the two dated measurements together ARE the evidence that this is OS-dependent — exactly C63's drift, and exactly the road C56/C72 travelled for 4:2:2. Consequence (1.16.7, WO 4.1's precedent applied): gate (g) stopped asserting silence and now reports both dated measurements and tells the operator to prove it on the target machine; the verdict CLASS is unchanged (advisory REVIEW). The OTI half is untouched — it was always a source read. Still open: `.mp2` is not an Apple-documented sample entry, so the spec-conformance objection survives the playability reversal and belongs in any sidecar |
| C103 | The `--fidelity` SSIM band is SCAN-DEPENDENT: healthy interlaced material scores materially lower than progressive, the deficit lives in the CHROMA planes rather than in field structure, and no field-normalization filter recovers it. | scripts/playable-check.sh (scan-keyed threshold + Y/U/V split); references/known-limits.md ("Interlaced sources sit in a lower fidelity-SSIM band") | player behavior + swscale/filter behavior — empirical only | healthy interlaced 4:2:2 clip through `--fidelity`; then the normalization sweep (bwdif/yadif/setfield/scale interl=0/8-bit chroma/neighbour) on the same pair | CONFIRMED 2026-08-15 on this bench: healthy synthetic interlaced 4:2:2 = All 0.8669 (Y 0.8892 / U 0.8527 / V 0.8588) — a FAIL against the 0.90 progressive default; best temporally-aligned frame Y 0.9678 vs U 0.8441 / V 0.8461. Normalization sweep: bwdif-ref 0.8669 (no-op), yadif-ref 0.8718, bwdif-both 0.8661, setfield=prog 0.8669, scale interl=0 0.8669, 8-bit chroma 0.8669, neighbour 0.8612 — all ≤0.005. Corroborated by the field report's real capture (healthy 0.8866–0.9684 vs corrupt 0.8146–0.8471). RESIDUAL: the interlaced floor (0.86) separates bands ~0.02 apart — thin, and stated as such in known-limits.md |
| C104 | A muxer can drop a mapped stream silently: `ffmpeg -c copy -f mov` was measured writing 2 of 3 mapped streams with nothing but a `-v warning` line, and every downstream gate passed because each one only examines the streams that ARE present. Therefore a post-mux stream census (count + per-stream codec identity, run on the part file before blessing) is the only thing standing between that loss and a green verdict. | scripts/lib-mux.sh (`mux_census`); every builder; SKILL.md (`RMX_CENSUS`) | ffmpeg behavior — empirical only | field report 2026-08-15 (the measured drop); regression: mux a 3-stream source, assert `RMX_CENSUS … match=ok`, and assert a deliberately wrong plan produces MISMATCH + exit 1 | CONFIRMED (the drop) by the field report 2026-08-15; the census itself is pinned by tests/regression.d/45-census-partnames.sh. NOT reproduced synthetically on this bench — the drop needed the real capture — so the pin proves the ASSERTION works, not the muxer bug |
| C105 | Quick Look does not follow symlinks: `qlmanage -t` on a symlink named `.mov` produces nothing and hits the deadline, while the identical HARDLINK thumbnails immediately — so an extension-keyed probe of a non-`.mov`-named file must hardlink, and a part file named `x.mov.part` fails `playable-check.sh` for a FILENAME reason that reads exactly like a decode failure. | scripts/playable-check.sh (extension guard); scripts/lib-mux.sh (`rtm_part`/`rtm_sidecar`); SKILL.md (house defaults) | macOS Quick Look behavior — empirical only | `qlmanage -t` on the same payload as (a) `x.mov.part`, (b) a `.mov` symlink, (c) a `.mov` hardlink | CONFIRMED 2026-08-15 on macOS 26.6.1: (a) no PNG, deadline hit; (b) no PNG, deadline hit; (c) PNG produced immediately. Pinned by tests/regression.d/45-census-partnames.sh |

## Gaps noticed (claims the audit plan's phases do not currently cover)

Surfaced by this sweep; per the plan's Phase 0 rule, each should be appended as
a Phase item or explicitly waived.

1. **stco/co64 >4 GiB (C46)** — no phase probes large-file chunk-offset
   behavior; Phase 5b touches multi-GB captures only for scan *cost*.
2. **faststart/moov placement (C45)** — no phase verifies atom order or the
   progressive-start claim on a built artifact.
3. **Legacy ©-atom readability (C44)** — Phase 4d verifies the `mdta` tree but
   nothing tests the "QuickTime reads some ©-atoms" half of the metadata story.
4. **MP3-in-MOV playability (C33)** and the wider audio playability matrix
   (C53–C59) — Phase 4a listens to the dual-track case only; no phase walks the
   `ingest-compatibility.md` "QuickTime plays it?" column on a real Mac.
5. **Mediabunny qt-carriage escape hatch (C60)** — verified once 2026-06-10,
   never pinned; no phase re-verifies it against new Mediabunny/ffmpeg versions.
6. **Subtitle-track timescale overflow (C13, second half)** — Phase 1c covers
   the *video* timescale assumption; the "no clean per-subtitle knob" claim for
   `mov_text` overflow is unprobed.
7. **Timecode track (C52)** — `-timecode` in delivery-encode.md writes a `tmcd`
   track no phase inspects.
8. **Malformed-atom rejection rules (C47) and wrong-handler claim (C49)** —
   stated as validation doctrine in container-internals.md, never exercised.
9. **Fragmented-input defragmentation (C50) and DRM detection (C51)** — edge
   cases stated with no probe in any phase.
10. **Dual-track two-pass alignment bound (~0.5 s offset from single-pass
    `-ss`)** — dual-track-quicktime.md states a measurable container-level
    offset mechanism (whole-frame trim on the copied track vs sample-exact trim
    on the decoded track) that no phase measures.
11. **Per-track delay preservation (C12)** — Phase 1d covers edit lists
    generally but not the specific "AC-3 delay survives copy" acceptance the
    docs promise.
